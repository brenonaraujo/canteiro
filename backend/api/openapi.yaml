openapi: 3.0.3
info:
  title: Canteiro API
  version: 0.5.0
  description: |
    F1 Conta: identidade Google, perfil mínimo, logout e desativação.
    F2 Catálogo: publicar, busca pública, operador declarado,
    calendário de disponibilidade e ficha pública.
    F3 Reserva: criar intent, autorizar pagamento (Stripe), webhook,
    aceitar/recusar pelo proprietário dentro da janela de 12h,
    recibo imutável do locatário.
    F4 Cancelamento + liquidação: política declarativa por ator e
    janela (locatário ≥24h/<24h/pós-início, proprietário, plataforma),
    split em 3 caixinhas (aluguel/horas/caução) com comissão 12%
    apenas sobre aluguel+horas, recibo imutável discriminado,
    webhook idempotente cobrindo refund/chargeback/deposit events,
    bloqueio de conta do locatário em chargeback, lock serializável
    cancelamento ↔ caução.
  contact:
    name: Canteiro
    url: https://canteiro.brenon.cloud
  license:
    name: MIT
servers:
  - url: http://localhost:8080
    description: Local
tags:
  - name: ops
    description: Liveness, readiness
  - name: auth
    description: Google identity and session
  - name: account
    description: Profile and deactivation
  - name: listings
    description: Owner CRUD for listings (F2)
  - name: catalog
    description: Public discovery, calendar and ficha (F2)
  - name: rentals
    description: Tenant reservations and receipts (F3)
  - name: payments
    description: Payment authorization webhook (F3)
  - name: cancellations
    description: Cancellation policy + liquidation (F4)
paths:
  /healthz:
    get:
      operationId: Healthz
      tags: [ops]
      summary: Liveness probe
      description: |
        Returns 200 when the HTTP process is alive.
        Does not check backing services.
      responses:
        "200":
          description: Alive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /readyz:
    get:
      operationId: Readyz
      tags: [ops]
      summary: Readiness probe
      description: |
        Returns 200 when backing services accept traffic.
        Returns 503 otherwise. Error payloads are generic (no DSN/PII).
        Localized `message` follows Accept-Language (en, pt-BR, es).
        Default locale is pt-BR.
      responses:
        "200":
          description: Ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadyResponse"
        "503":
          description: Not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadyResponse"
  /auth/google:
    get:
      operationId: StartGoogleAuth
      tags: [auth]
      summary: Start Google sign-in
      description: |
        Redirects to Google. Only Google creates a v1 account.
        Interrupted or refused consent creates neither account nor session.
      responses:
        "302":
          description: Redirect to Google
          headers:
            Location:
              schema:
                type: string
        "503":
          description: Identity provider is not configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /auth/google/callback:
    get:
      operationId: GoogleCallback
      tags: [auth]
      summary: Google OAuth callback
      description: |
        Exchanges a valid Google code, creates or reopens the account,
        sets a session cookie, and redirects to the web app.
        Query params `code`, `state`, and `error` are read from the URL.
        The same Google subject never creates a second account.
        A deactivated account is reopened still deactivated.
      responses:
        "302":
          description: Redirect to the web app
          headers:
            Location:
              schema:
                type: string
  /auth/logout:
    post:
      operationId: Logout
      tags: [auth]
      summary: End the current session
      description: |
        Ends authenticated access. Does not deactivate the account
        and does not hide listings.
      security:
        - cookieAuth: []
      responses:
        "204":
          description: Session ended
        "401":
          $ref: "#/components/responses/Unauthorized"
  /account:
    get:
      operationId: GetAccount
      tags: [account]
      summary: Current account
      description: |
        Returns the caller account. Never includes Google subject,
        email, or other identifiers beyond the public profile.
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Current account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: UpdateAccount
      tags: [account]
      summary: Update visible name and phone
      description: |
        Empty name or phone is rejected. Completing both makes the
        account eligible to reserve (F3). Publishing stays blocked (F2).
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAccountRequest"
      responses:
        "200":
          description: Updated account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /account/deactivate:
    post:
      operationId: DeactivateAccount
      tags: [account]
      summary: Deactivate own account
      description: |
        Irreversible in v1. Listings leave public search (F2 invariant).
        In-progress rentals are not cancelled (F3 invariant).
        Requires confirm=true.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeactivateAccountRequest"
      responses:
        "200":
          description: Deactivated account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /listings:
    get:
      operationId: ListMineListings
      tags: [listings]
      summary: List the caller's listings
      description: |
        Returns every listing owned by the authenticated account,
        regardless of state. Used for the owner dashboard.
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Listings owned by the caller
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Listing"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: CreateListingDraft
      tags: [listings]
      summary: Create a draft listing
      description: |
        Creates a listing in `draft`. Drafts are not visible in public
        search. The owner must satisfy publish gates (photo, deposit
        minimum, cession declaration when heavy, operator identification
        when operator is required, valid delivery coverage when delivery
        is declared, payout details and owner terms) before the listing
        can move to `published`. Deactivated accounts cannot create
        listings; accounts without visible name and phone can persist a
        draft but cannot publish it.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateListingRequest"
      responses:
        "201":
          description: Draft created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Listing"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /listings/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: GetMyListing
      tags: [listings]
      summary: Get one of the caller's listings (private)
      description: |
        Returns the listing with full owner data including operator
        identity and contact (only used by the owner).
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Listing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Listing"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: UpdateListing
      tags: [listings]
      summary: Update a draft or paused listing
      description: |
        Updates listing fields while in `draft` or `paused`. A published
        listing cannot be edited through this endpoint; the owner must
        pause it first, edit it, then publish again.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateListingRequest"
      responses:
        "200":
          description: Updated listing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Listing"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Listing is published; pause it before editing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /listings/{id}/publish:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: PublishListing
      tags: [listings]
      summary: Publish (or unpause) a listing
      description: |
        Moves a draft or paused listing into `published`. All publish
        gates are evaluated server-side. A listing that fails any gate
        stays in its current state and returns 422 with the missing field
        codes. Pausing is reversible by calling this endpoint again.
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Listing published (or unpaused)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Listing"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /listings/{id}/pause:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: PauseListing
      tags: [listings]
      summary: Pause a published listing
      description: |
        Pauses a published listing. It is removed from public search
        but data is preserved. It is not deleted.
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Listing paused
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Listing"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Listing is not published
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /listings/{id}/blocks:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: ListBlocks
      tags: [listings]
      summary: Owner view of availability blocks
      description: |
        Returns every availability block created by the owner. Used by
        the owner dashboard to manage the calendar.
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Blocks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AvailabilityBlock"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: AddBlock
      tags: [listings]
      summary: Add an availability block
      description: |
        Adds a block to the listing calendar. Blocks must not overlap
        (the calendar is the single source of availability; F3 will
        honour this invariant on reservation).
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddBlockRequest"
      responses:
        "201":
          description: Block created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvailabilityBlock"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Block overlaps an existing block
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /listings/{id}/blocks/{blockId}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      - in: path
        name: blockId
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: RemoveBlock
      tags: [listings]
      summary: Remove an availability block
      description: |
        Removes a block created by the owner. Past blocks are kept
        for the audit log.
      security:
        - cookieAuth: []
      responses:
        "204":
          description: Block removed
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /owner/onboarding:
    get:
      operationId: GetOwnerOnboarding
      tags: [listings]
      summary: Owner onboarding state
      description: |
        Returns whether the owner has set payout details and accepted
        the owner terms. Both must be true before the owner can publish
        a listing.
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Owner onboarding state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OwnerOnboarding"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: UpdateOwnerOnboarding
      tags: [listings]
      summary: Update payout details and/or accept terms
      description: |
        Updates owner-side onboarding data. Setting `accept_terms=true`
        records the acceptance timestamp and locks the published owner
        terms version. Payout details are stored as opaque strings in
        v1 (the actual Stripe Connect integration lands in F3).
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOwnerOnboardingRequest"
      responses:
        "200":
          description: Owner onboarding updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OwnerOnboarding"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /catalog/listings:
    get:
      operationId: SearchCatalog
      tags: [catalog]
      summary: Public search (no login required)
      description: |
        Search published listings. Supports filters by category, city,
        date interval (returning listings not blocked across the entire
        interval), operator mode, price range and size (light or heavy).
        Pages are 1-indexed with a fixed page size.
      parameters:
        - in: query
          name: category
          schema:
            $ref: "#/components/schemas/ListingCategory"
        - in: query
          name: city
          schema:
            type: string
        - in: query
          name: from
          schema:
            type: string
            format: date
        - in: query
          name: to
          schema:
            type: string
            format: date
        - in: query
          name: operator_mode
          schema:
            $ref: "#/components/schemas/OperatorMode"
        - in: query
          name: size
          schema:
            $ref: "#/components/schemas/ListingSize"
        - in: query
          name: min_price_cents
          schema:
            type: integer
            minimum: 0
        - in: query
          name: max_price_cents
          schema:
            type: integer
            minimum: 0
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        "200":
          description: Page of public listings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListingPage"
  /catalog/listings/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: GetPublicListing
      tags: [catalog]
      summary: Public ficha (no login required)
      description: |
        Returns the public ficha for a published listing. Strips owner
        identity and operator identity/contact; only the public contact
        field (pix key, phone) declared on the publication is hidden
        until F3.
      responses:
        "200":
          description: Public ficha
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicListing"
        "404":
          $ref: "#/components/responses/NotFound"
  /catalog/listings/{id}/calendar:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: GetPublicCalendar
      tags: [catalog]
      summary: Public calendar (no login required)
      description: |
        Returns the list of date intervals the listing is unavailable,
        plus the minimum reservation lead time in hours. Does not reveal
        who reserved any interval.
      parameters:
        - in: query
          name: from
          schema:
            type: string
            format: date
        - in: query
          name: to
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Public calendar
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicCalendar"
        "404":
          $ref: "#/components/responses/NotFound"
  /catalog/categories:
    get:
      operationId: ListCategories
      tags: [catalog]
      summary: Listing categories (config)
      description: |
        Returns the catalog of categories, including the deposit minimum
        per category (cents) required for publication.
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CategoryConfig"
  /rentals:
    post:
      operationId: CreateRental
      tags: [rentals]
      summary: Create a reservation intent (tenant)
      description: |
        Validates the preconditions (tenant active, listing published,
        no calendar overlap, operator terms accepted when required), prices
        the rental and persists a `pending` rental. Idempotent on the
        (tenant, listing, window) tuple.
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRentalRequest"
      responses:
        "201":
          description: Rental created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RentalQuoteOut"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
    get:
      operationId: ListMyRentals
      tags: [rentals]
      summary: List the caller's rentals
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Rentals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Rental"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /rentals/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: GetRental
      tags: [rentals]
      summary: Get one rental
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Rental
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rental"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /rentals/{id}/authorize:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: AuthorizeRentalPayment
      tags: [rentals]
      summary: Authorize payment (tenant)
      security:
        - cookieAuth: []
      responses:
        "200":
          description: PaymentIntent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentIntent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /rentals/{id}/accept:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: AcceptRental
      tags: [rentals]
      summary: Accept the rental (owner)
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Rental confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rental"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /rentals/{id}/decline:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: DeclineRental
      tags: [rentals]
      summary: Decline the rental (owner)
      security:
        - cookieAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeclineRentalRequest"
      responses:
        "200":
          description: Rental declined
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rental"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /rentals/{id}/cancel:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: CancelRental
      tags: [rentals]
      summary: Cancel the rental pre-acceptance (tenant)
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Rental cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rental"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /rentals/{id}/receipt:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: GetRentalReceipt
      tags: [rentals]
      summary: Tenant receipt (immutable)
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Receipt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RentalReceipt"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /rentals/{id}/cancellations:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: CreateRentalCancellation
      tags: [cancellations]
      summary: Cancel a rental (tenant, owner, or platform)
      description: |
        Applies the F4 cancellation policy (AC-1..AC-8). The caller
        must be the tenant, the listing owner, or a platform operator.
        The classification is purely declarative — derived from
        (actor, time-to-pickup, started?, operator identity) — and
        the resulting split (aluguel/horas/caução) and commission
        (12% over rent+operator, deposit always out) is persisted as
        an immutable cancellation record + receipt.
        The rental moves to `cancellation_in_progress` and then to
        `cancelled` (terminal). Idempotent: a second call with the
        same `actor_kind` returns the existing record (409 if
        `actor_kind` differs).
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRentalCancellationRequest"
      responses:
        "200":
          description: Cancellation accepted (idempotent replay)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RentalCancellation"
        "201":
          description: Cancellation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RentalCancellation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
    get:
      operationId: GetRentalCancellation
      tags: [cancellations]
      summary: Get the cancellation record (immutable, both parties)
      description: |
        Returns the cancellation record once a rental was cancelled.
        Visible to the tenant and the listing owner. Returns 404
        when no cancellation exists yet. The record is immutable:
        correction is a new event referencing the original.
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Cancellation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RentalCancellation"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /payments/webhook:
    post:
      operationId: PaymentWebhook
      tags: [payments]
      summary: PSP webhook (signature verified server-side)
      parameters:
        - in: header
          name: Stripe-Signature
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentWebhookEvent"
      responses:
        "200":
          description: Event accepted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /rentals/{id}/pickup:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: RegisterRentalPickup
      tags: [rentals]
      summary: Register pickup state (either party)
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnEvidence"
      responses:
        "200":
          description: Return row created (in_progress)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Return"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /rentals/{id}/return:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: RegisterRentalReturn
      tags: [rentals]
      summary: Register return state and close the rental (either party)
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnEvidence"
      responses:
        "200":
          description: Return closed (deposit released)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Return"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /rentals/{id}/damage:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: OpenDamageClaim
      tags: [damage]
      summary: Open a damage claim for the rental (owner, 48h window)
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenDamageClaimRequest"
      responses:
        "201":
          description: Claim opened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DamageClaim"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /damage/{id}/respond:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: RespondDamageClaim
      tags: [damage]
      summary: Renter response to an open claim (48h window)
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RespondDamageClaimRequest"
      responses:
        "200":
          description: Claim updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DamageClaim"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /damage/{id}/resolve:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: StaffResolveDamage
      tags: [damage]
      summary: Staff resolution of a contested claim (final in v1)
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffResolveDamageRequest"
      responses:
        "200":
          description: Claim resolved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DamageClaim"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /debt/{id}/settle:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: SettleDebt
      tags: [debt]
      summary: Mark a debt as paid (renter)
      security:
        - cookieAuth: []
      responses:
        "200":
          description: Debt settled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Debt"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
  /debt/{id}/forgive:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: ForgiveDebt
      tags: [debt]
      summary: Forgive a debt (staff; reason mandatory)
      security:
        - cookieAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForgiveDebtRequest"
      responses:
        "200":
          description: Debt updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Debt"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/Unprocessable"
components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: canteiro_session
  schemas:
    HealthResponse:
      type: object
      required: [status]
      properties:
        status:
          type: string
          example: ok
        service:
          type: string
          example: canteiro
    ReadyResponse:
      type: object
      required: [status, message, message_key]
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        service:
          type: string
          example: canteiro
        message:
          type: string
          description: Localized human message
        message_key:
          type: string
          description: Stable i18n key
          example: ready.ok
    Error:
      type: object
      required: [code, message, message_key]
      properties:
        code:
          type: string
          example: unauthorized
        message:
          type: string
          description: Localized human message
        message_key:
          type: string
          example: auth.unauthorized
    Account:
      type: object
      required: [id, status, display_name, phone, capabilities]
      properties:
        id:
          type: string
          description: Account id (UUID)
        status:
          type: string
          enum: [incomplete, active, deactivated, blocked]
        display_name:
          type: string
        phone:
          type: string
        capabilities:
          $ref: "#/components/schemas/AccountCapabilities"
    AccountCapabilities:
      type: object
      required: [reserve, publish]
      properties:
        reserve:
          type: boolean
          description: True when profile is complete and account is active
        publish:
          type: boolean
          description: Always false in F1 (owner onboarding is F2)
    UpdateAccountRequest:
      type: object
      required: [display_name, phone]
      properties:
        display_name:
          type: string
          minLength: 1
          maxLength: 80
          example: Ana
        phone:
          type: string
          minLength: 1
          maxLength: 32
          example: "+5511999999999"
    DeactivateAccountRequest:
      type: object
      required: [confirm]
      properties:
        confirm:
          type: boolean
          example: true
    ListingCategory:
      type: string
      enum: [manual, electric, light_construction, agricultural, heavy]
    ListingSize:
      type: string
      enum: [light, heavy]
    OperatorMode:
      type: string
      enum: [none, optional, required]
    PriceUnit:
      type: string
      enum: [hour, day]
    ListingState:
      type: string
      enum: [draft, published, paused]
    OperatorIdentity:
      type: object
      required: [name, phone, is_owner]
      properties:
        name:
          type: string
        phone:
          type: string
        is_owner:
          type: boolean
          description: True when the operator is the listing owner.
    ListingRules:
      type: object
      properties:
        document_required:
          type: boolean
        min_age:
          type: integer
          minimum: 0
          maximum: 130
        experience_required:
          type: boolean
        travel_restricted:
          type: boolean
    ListingDelivery:
      type: object
      required: [enabled, coverage]
      properties:
        enabled:
          type: boolean
        coverage:
          type: string
          description: Free-text delivery area; non-empty when enabled.
    ListingOperator:
      type: object
      required: [mode]
      properties:
        mode:
          $ref: "#/components/schemas/OperatorMode"
        hourly_rate_cents:
          type: integer
          minimum: 0
        min_hours:
          type: integer
          minimum: 1
        identity:
          $ref: "#/components/schemas/OperatorIdentity"
    Listing:
      type: object
      required:
        - id
        - owner_account_id
        - state
        - title
        - description
        - category
        - pickup_city
        - pickup_neighborhood
        - delivery
        - price_unit
        - price_amount_cents
        - deposit_cents
        - min_lead_time_hours
        - photos
        - rules
        - operator
      properties:
        id:
          type: string
          format: uuid
        owner_account_id:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/ListingState"
        title:
          type: string
          maxLength: 120
        description:
          type: string
          maxLength: 4000
        category:
          $ref: "#/components/schemas/ListingCategory"
        size:
          $ref: "#/components/schemas/ListingSize"
        pickup_city:
          type: string
          maxLength: 80
        pickup_neighborhood:
          type: string
          maxLength: 80
        delivery:
          $ref: "#/components/schemas/ListingDelivery"
        price_unit:
          $ref: "#/components/schemas/PriceUnit"
        price_amount_cents:
          type: integer
          minimum: 0
        deposit_cents:
          type: integer
          minimum: 0
        min_lead_time_hours:
          type: integer
          minimum: 0
        photos:
          type: array
          minItems: 1
          items:
            type: string
            description: Image URL or stored object key.
        rules:
          $ref: "#/components/schemas/ListingRules"
        operator:
          $ref: "#/components/schemas/ListingOperator"
        heavy_legal_cession:
          type: boolean
          description: Required true for `heavy` category when published.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateListingRequest:
      type: object
      required:
        - title
        - description
        - category
        - pickup_city
        - pickup_neighborhood
        - price_unit
        - price_amount_cents
        - deposit_cents
        - min_lead_time_hours
        - operator
      properties:
        title:
          type: string
          minLength: 4
          maxLength: 120
        description:
          type: string
          minLength: 12
          maxLength: 4000
        category:
          $ref: "#/components/schemas/ListingCategory"
        pickup_city:
          type: string
          minLength: 1
          maxLength: 80
        pickup_neighborhood:
          type: string
          maxLength: 80
        delivery:
          $ref: "#/components/schemas/ListingDelivery"
        price_unit:
          $ref: "#/components/schemas/PriceUnit"
        price_amount_cents:
          type: integer
          minimum: 1
        deposit_cents:
          type: integer
          minimum: 0
        min_lead_time_hours:
          type: integer
          minimum: 0
        photos:
          type: array
          items:
            type: string
        rules:
          $ref: "#/components/schemas/ListingRules"
        operator:
          $ref: "#/components/schemas/ListingOperator"
        heavy_legal_cession:
          type: boolean
    UpdateListingRequest:
      type: object
      properties:
        title:
          type: string
          minLength: 4
          maxLength: 120
        description:
          type: string
          minLength: 12
          maxLength: 4000
        category:
          $ref: "#/components/schemas/ListingCategory"
        pickup_city:
          type: string
          maxLength: 80
        pickup_neighborhood:
          type: string
          maxLength: 80
        delivery:
          $ref: "#/components/schemas/ListingDelivery"
        price_unit:
          $ref: "#/components/schemas/PriceUnit"
        price_amount_cents:
          type: integer
          minimum: 1
        deposit_cents:
          type: integer
          minimum: 0
        min_lead_time_hours:
          type: integer
          minimum: 0
        photos:
          type: array
          items:
            type: string
        rules:
          $ref: "#/components/schemas/ListingRules"
        operator:
          $ref: "#/components/schemas/ListingOperator"
        heavy_legal_cession:
          type: boolean
    PublicListing:
      type: object
      required:
        - id
        - title
        - description
        - category
        - size
        - pickup_city
        - pickup_neighborhood
        - delivery
        - price_unit
        - price_amount_cents
        - deposit_cents
        - min_lead_time_hours
        - photos
        - rules
        - operator
        - created_at
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        category:
          $ref: "#/components/schemas/ListingCategory"
        size:
          $ref: "#/components/schemas/ListingSize"
        pickup_city:
          type: string
        pickup_neighborhood:
          type: string
        delivery_enabled:
          type: boolean
        price_unit:
          $ref: "#/components/schemas/PriceUnit"
        price_amount_cents:
          type: integer
        deposit_cents:
          type: integer
        min_lead_time_hours:
          type: integer
        photos:
          type: array
          items:
            type: string
        rules:
          $ref: "#/components/schemas/ListingRules"
        operator_mode:
          $ref: "#/components/schemas/OperatorMode"
        operator_hourly_rate_cents:
          type: integer
        created_at:
          type: string
          format: date-time
    ListingPage:
      type: object
      required: [page, page_size, total, items]
      properties:
        page:
          type: integer
        page_size:
          type: integer
        total:
          type: integer
        items:
          type: array
          items:
            $ref: "#/components/schemas/PublicListing"
    AvailabilityBlock:
      type: object
      required: [id, listing_id, starts_at, ends_at, reason, created_at]
      properties:
        id:
          type: string
          format: uuid
        listing_id:
          type: string
          format: uuid
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: string
          format: date-time
        reason:
          type: string
          description: Free-text owner-side reason.
        created_at:
          type: string
          format: date-time
    AddBlockRequest:
      type: object
      required: [starts_at, ends_at]
      properties:
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: string
          format: date-time
        reason:
          type: string
          maxLength: 240
    PublicCalendar:
      type: object
      required: [listing_id, min_lead_time_hours, blocks]
      properties:
        listing_id:
          type: string
          format: uuid
        min_lead_time_hours:
          type: integer
        blocks:
          type: array
          items:
            type: object
            required: [starts_at, ends_at]
            properties:
              starts_at:
                type: string
                format: date-time
              ends_at:
                type: string
                format: date-time
    CategoryConfig:
      type: object
      required: [category, size, deposit_min_cents]
      properties:
        category:
          $ref: "#/components/schemas/ListingCategory"
        size:
          $ref: "#/components/schemas/ListingSize"
        deposit_min_cents:
          type: integer
          minimum: 0
    OwnerOnboarding:
      type: object
      required: [payout_set, terms_accepted, terms_version]
      properties:
        payout_set:
          type: boolean
        terms_accepted:
          type: boolean
        terms_version:
          type: string
        terms_accepted_at:
          type: string
          format: date-time
        payout_kind:
          type: string
          description: Opaque payout kind (e.g. pix, bank).
        payout_last4:
          type: string
    UpdateOwnerOnboardingRequest:
      type: object
      properties:
        payout_kind:
          type: string
        payout_last4:
          type: string
        accept_terms:
          type: boolean
        terms_version:
          type: string
    RentalState:
      type: string
      enum: [pending, authorized, confirmed, declined, expired, cancelled, cancellation_in_progress, refunded]
    Rental:
      type: object
      required:
        - id
        - listing_id
        - tenant_account_id
        - state
        - starts_at
        - ends_at
        - with_operator
        - operator_terms_accepted
        - intent_key
        - rent_cents
        - operator_cents
        - deposit_cents
        - commission_cents
        - owner_payout_cents
        - operator_payout_cents
        - listing_snapshot
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        listing_id:
          type: string
          format: uuid
        tenant_account_id:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/RentalState"
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: string
          format: date-time
        with_operator:
          type: boolean
        operator_terms_accepted:
          type: boolean
        intent_key:
          type: string
        rent_cents:
          type: integer
          minimum: 0
        operator_cents:
          type: integer
          minimum: 0
        deposit_cents:
          type: integer
          minimum: 0
        commission_cents:
          type: integer
          minimum: 0
        owner_payout_cents:
          type: integer
          minimum: 0
        operator_payout_cents:
          type: integer
          minimum: 0
        listing_snapshot:
          $ref: "#/components/schemas/ListingSnapshot"
        acceptance_deadline_at:
          type: string
          format: date-time
        confirmed_at:
          type: string
          format: date-time
        declined_at:
          type: string
          format: date-time
        decline_reason:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ListingSnapshot:
      type: object
      required: [owner_id, title, category, price_unit, price_amount_cents, deposit_cents, operator]
      properties:
        owner_id:
          type: string
          format: uuid
        title:
          type: string
        category:
          $ref: "#/components/schemas/ListingCategory"
        price_unit:
          $ref: "#/components/schemas/PriceUnit"
        price_amount_cents:
          type: integer
          minimum: 0
        deposit_cents:
          type: integer
          minimum: 0
        min_lead_time_hours:
          type: integer
          minimum: 0
        pickup_city:
          type: string
        operator:
          $ref: "#/components/schemas/SnapshotOperator"
    SnapshotOperator:
      type: object
      required: [mode, hourly_rate_cents, min_hours, is_owner]
      properties:
        mode:
          $ref: "#/components/schemas/OperatorMode"
        hourly_rate_cents:
          type: integer
          minimum: 0
        min_hours:
          type: integer
          minimum: 0
        name:
          type: string
        phone:
          type: string
        is_owner:
          type: boolean
    CreateRentalRequest:
      type: object
      required: [listing_id, starts_at, ends_at, with_operator, operator_terms_accepted]
      properties:
        listing_id:
          type: string
          format: uuid
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: string
          format: date-time
        with_operator:
          type: boolean
        operator_terms_accepted:
          type: boolean
    RentalQuoteOut:
      type: object
      required:
        - rental_id
        - rent_cents
        - operator_cents
        - deposit_cents
        - total_cents
        - commission_base_cents
        - commission_cents
        - owner_payout_cents
        - operator_payout_cents
      properties:
        rental_id:
          type: string
          format: uuid
        rent_cents:
          type: integer
          minimum: 0
        operator_cents:
          type: integer
          minimum: 0
        deposit_cents:
          type: integer
          minimum: 0
        total_cents:
          type: integer
          minimum: 0
        commission_base_cents:
          type: integer
          minimum: 0
        commission_cents:
          type: integer
          minimum: 0
        owner_payout_cents:
          type: integer
          minimum: 0
        operator_payout_cents:
          type: integer
          minimum: 0
    DeclineRentalRequest:
      type: object
      properties:
        reason:
          type: string
          enum: [outside_schedule, item_unavailable, terms_mismatch, other]
    RentalReceipt:
      type: object
      required:
        - rental_id
        - tenant_account_id
        - rent_cents
        - operator_cents
        - deposit_cents
        - total_cents
        - commission_base_cents
        - commission_cents
        - owner_payout_cents
        - operator_payout_cents
        - listing_snapshot
        - window_starts_at
        - window_ends_at
        - issued_at
      properties:
        rental_id:
          type: string
          format: uuid
        tenant_account_id:
          type: string
          format: uuid
        rent_cents:
          type: integer
        operator_cents:
          type: integer
        deposit_cents:
          type: integer
        total_cents:
          type: integer
        commission_base_cents:
          type: integer
        commission_cents:
          type: integer
        owner_payout_cents:
          type: integer
        operator_payout_cents:
          type: integer
        listing_snapshot:
          $ref: "#/components/schemas/ListingSnapshot"
        window_starts_at:
          type: string
          format: date-time
        window_ends_at:
          type: string
          format: date-time
        issued_at:
          type: string
          format: date-time
        # F4 — AC-11 immutable liquidation receipt.
        actor_kind:
          type: string
          enum: [tenant, owner, platform, operator]
          description: Who triggered the cancellation (F4). Empty when no cancellation.
        window_applied:
          type: string
          description: |
            F4 declarative window code (e.g. "tenant.pre_accept",
            "tenant.ge_24h", "tenant.lt_24h", "tenant.after_start",
            "owner.pre_pickup", "owner.after_start").
          example: "tenant.ge_24h"
        cancellation_fee_cents:
          type: integer
          minimum: 0
          description: F4 cancellation fee (10% of rent in the >=24h window).
        tenant_refund_cents:
          type: integer
          description: Net amount refunded to the tenant after fee + commission.
        owner_payout_cents_after_cancellation:
          type: integer
          description: Owner payout net of commission (zero when rent fully refunded).
        operator_payout_cents_after_cancellation:
          type: integer
          description: Operator payout (third-party operator; zero when operator=owner).
        deposit_state:
          type: string
          enum: [released, captured, partial, held]
          description: |
            F4 deposit state in the receipt: released (default), captured
            (fully), partial (partially), held (still held; F5 will decide).
        deposit_capture_cents:
          type: integer
          minimum: 0
        deposit_release_cents:
          type: integer
          minimum: 0
        deposit_partial_capture_cents:
          type: integer
          minimum: 0
        processor_operation_id:
          type: string
          description: PSP-side operation id (refund, chargeback, deposit capture).
        cancellation_issued_at:
          type: string
          format: date-time
    RentalCancellation:
      type: object
      required:
        - id
        - rental_id
        - actor_account_id
        - actor_kind
        - window_applied
        - state_deposit
        - cancellation_fee_cents
        - tenant_refund_cents
        - owner_payout_cents_after_cancellation
        - operator_payout_cents_after_cancellation
        - deposit_capture_cents
        - deposit_release_cents
        - deposit_partial_capture_cents
        - issued_at
      properties:
        id:
          type: string
          format: uuid
        rental_id:
          type: string
          format: uuid
        actor_account_id:
          type: string
          format: uuid
        actor_kind:
          type: string
          enum: [tenant, owner, platform, operator]
        window_applied:
          type: string
          description: Declarative F4 window code (see RentalReceipt.window_applied).
        state_deposit:
          type: string
          enum: [released, captured, partial, held]
        cancellation_fee_cents:
          type: integer
          minimum: 0
        tenant_refund_cents:
          type: integer
        owner_payout_cents_after_cancellation:
          type: integer
        operator_payout_cents_after_cancellation:
          type: integer
        deposit_capture_cents:
          type: integer
          minimum: 0
        deposit_release_cents:
          type: integer
          minimum: 0
        deposit_partial_capture_cents:
          type: integer
          minimum: 0
        processor_operation_id:
          type: string
        reversal_reason:
          type: string
          description: Set when this cancellation is a chargeback reversal.
        issued_at:
          type: string
          format: date-time
    CreateRentalCancellationRequest:
      type: object
      required: [actor_kind]
      properties:
        actor_kind:
          type: string
          enum: [tenant, owner, platform, operator]
        reason:
          type: string
          maxLength: 240
        processor_operation_id:
          type: string
          description: |
            Optional PSP-side operation id that triggered the
            cancellation (refund, chargeback). When the request comes
            from a webhook handler, this is the event id; idempotent
            on (rental_id, actor_kind, processor_operation_id).
    PaymentIntent:
      type: object
      required:
        - id
        - rental_id
        - provider
        - idempotency_key
        - attempt
        - amount_cents
        - deposit_cents
        - expected_total_cents
        - status
      properties:
        id:
          type: string
          format: uuid
        rental_id:
          type: string
          format: uuid
        provider:
          type: string
          enum: [stripe, noop]
        provider_payment_id:
          type: string
        idempotency_key:
          type: string
        attempt:
          type: integer
          minimum: 1
        amount_cents:
          type: integer
          minimum: 0
        deposit_cents:
          type: integer
          minimum: 0
        expected_total_cents:
          type: integer
          minimum: 0
        status:
          type: string
          enum: [requires_action, authorized, failed, refunded]
        failure_code:
          type: string
        failure_message:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    PaymentWebhookEvent:
      type: object
      required: [id, type, data]
      properties:
        id:
          type: string
        type:
          type: string
          enum:
            - payment.authorized
            - payment.failed
            - payment.refunded
            - payment.refund_settled
            - payment.chargeback_created
            - payment.deposit_captured
            - payment.deposit_released
            - payment.deposit_partial_captured
        data:
          type: object
          required: [rental_id, amount_cents]
          properties:
            rental_id:
              type: string
              format: uuid
            payment_intent_id:
              type: string
              format: uuid
            amount_cents:
              type: integer
              minimum: 0
            deposit_cents:
              type: integer
              minimum: 0
            deposit_state:
              type: string
              enum: [released, captured, partial]
              description: |
                F4 deposit event — only set for deposit.* and
                refund_settled/chargeback events. Required for
                deposit events; ignored otherwise.
            chargeback_reason:
              type: string
              description: Set only on chargeback events.
            failure_code:
              type: string
            failure_message:
              type: string
    ReturnState:
      type: string
      enum: [awaiting_pickup, in_progress, awaiting_confirmation, closed, contested]
    Return:
      type: object
      required: [id, rental_id, state]
      properties:
        id:
          type: string
          format: uuid
        rental_id:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/ReturnState"
        pickup_evidence:
          $ref: "#/components/schemas/ReturnEvidence"
        return_evidence:
          $ref: "#/components/schemas/ReturnEvidence"
        deposit_released_cents:
          type: integer
          minimum: 0
        deposit_captured_cents:
          type: integer
          minimum: 0
        returned_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ReturnEvidence:
      type: object
      properties:
        photos:
          type: array
          items:
            type: string
        description:
          type: string
        checklist:
          type: array
          items:
            type: string
    DamageState:
      type: string
      enum: [open, renter_agreed, contested, staff_resolved, expired, cancelled]
    DamageNature:
      type: string
      enum: [cosmetic, functional, loss]
    DamageClaim:
      type: object
      required: [id, rental_id, state, nature]
      properties:
        id:
          type: string
          format: uuid
        rental_id:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/DamageState"
        nature:
          $ref: "#/components/schemas/DamageNature"
        description:
          type: string
        proposed_cents:
          type: integer
          minimum: 0
        agreed_cents:
          type: integer
          minimum: 0
        opened_at:
          type: string
          format: date-time
        responded_at:
          type: string
          format: date-time
        decided_at:
          type: string
          format: date-time
        resolved_at:
          type: string
          format: date-time
    OpenDamageClaimRequest:
      type: object
      required: [nature, proposed_cents, description]
      properties:
        nature:
          $ref: "#/components/schemas/DamageNature"
        proposed_cents:
          type: integer
          minimum: 1
        description:
          type: string
          minLength: 1
        evidence:
          $ref: "#/components/schemas/ReturnEvidence"
    RespondDamageClaimRequest:
      type: object
      required: [response]
      properties:
        response:
          type: string
          enum: [agree, contest, counter]
        agreed_cents:
          type: integer
          minimum: 1
        note:
          type: string
    StaffResolveDamageRequest:
      type: object
      required: [agreed_cents, note]
      properties:
        agreed_cents:
          type: integer
          minimum: 1
        note:
          type: string
          minLength: 1
    DebtState:
      type: string
      enum: [open, settled, forgiven]
    Debt:
      type: object
      required: [id, rental_id, state, original_cents]
      properties:
        id:
          type: string
          format: uuid
        rental_id:
          type: string
          format: uuid
        damage_id:
          type: string
          format: uuid
        renter_id:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/DebtState"
        original_cents:
          type: integer
          minimum: 1
        forgiven_cents:
          type: integer
          minimum: 0
        settled_cents:
          type: integer
          minimum: 0
        forgiven_reason:
          type: string
        due_at:
          type: string
          format: date-time
        settled_at:
          type: string
          format: date-time
        forgiven_at:
          type: string
          format: date-time
    ForgiveDebtRequest:
      type: object
      required: [cents, reason]
      properties:
        cents:
          type: integer
          minimum: 1
        reason:
          type: string
          minLength: 1
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: Conflict with current resource state
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Missing or unknown session
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Authenticated but not allowed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unprocessable:
      description: Semantic validation failed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
